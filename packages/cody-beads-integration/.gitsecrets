# Git Secrets configuration for detecting secrets in code
# This file prevents committing sensitive information like API keys, tokens, etc.

# Common patterns to detect
^[a-zA-Z_0-9]=?[a-zA-Z0-9]{20,}$
[0-9]{25,}
[0-9A-F]{32,}
[0-9A-Za-z/+]{40,}
[0-9A-Za-z/+]{50,}
[0-9A-Za-z/+]{60,}
[0-9A-Za-z/+]{70,}

# AWS patterns
AKIA[0-9A-Z]{16}
[0-9a-zA-Z/+]{40}
aws_access_key_id
aws_secret_access_key
aws_session_token

# GitHub patterns
github_token:
GITHUB_TOKEN=
ghp_[a-zA-Z0-9]{36}
gho_[a-zA-Z0-9]{36}
ghu_[a-zA-Z0-9]{36}
ghs_[a-zA-Z0-9]{36}
ghr_[a-zA-Z0-9]{36}
github_pat_[a-zA-Z0-9_]{82}

# API Keys
api_key:
API_KEY=
api_key =
API_KEY =
api_key:
APIKEY=
APIKEY

# Database URLs
mongodb://
postgresql://
mysql://
redis://

# JWT tokens
eyJ[0-9A-Za-z+/=]*\.[0-9A-Za-z+/=]*\.[0-9A-Za-z+/=]*

# Common secret keys
secret_key:
SECRET_KEY=
private_key:
PRIVATE_KEY=
password:
PASSWORD=
token:
TOKEN=

# Cody-specific patterns
cody_api_key:
CODY_API_KEY=
cody_token:
CODY_TOKEN=

# Beads-specific patterns
beads_token:
BEADS_TOKEN=
beads_key:
BEADS_KEY=